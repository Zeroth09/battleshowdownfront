# Database Configuration
MONGODB_URI=mongodb://localhost:27017/battle-games

# JWT Secret
JWT_SECRET=rahasia-jwt-super-secret-key

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (untuk CORS)
FRONTEND_URL=http://localhost:3000

# Railway Configuration (untuk deployment)
# PORT akan di-set otomatis oleh Railway
# MONGODB_URI akan di-set dari Railway MongoDB addon 